[
  {
    "id": "dropTable.agentRoleSettings",
    "label": "Drop table agentRoleSettings",
    "summary": "Drops table agentRoleSettings which is not in the contract",
    "operationClass": "destructive",
    "target": {
      "id": "sqlite",
      "details": {
        "schema": "main",
        "objectType": "table",
        "name": "agentRoleSettings"
      }
    },
    "precheck": [
      {
        "description": "ensure table \"agentRoleSettings\" exists",
        "sql": "SELECT COUNT(*) > 0 AS \"result\" FROM \"sqlite_master\" WHERE (\"type\" = ? AND \"name\" = ?)",
        "params": [
          "table",
          "agentRoleSettings"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop table \"agentRoleSettings\"",
        "sql": "DROP TABLE \"agentRoleSettings\""
      }
    ],
    "postcheck": [
      {
        "description": "verify table \"agentRoleSettings\" is gone",
        "sql": "SELECT COUNT(*) = 0 AS \"result\" FROM \"sqlite_master\" WHERE (\"type\" = ? AND \"name\" = ?)",
        "params": [
          "table",
          "agentRoleSettings"
        ]
      }
    ]
  }
]